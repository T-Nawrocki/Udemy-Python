# appending can be done by opening the file in "a" mode
# side note— "r", "w" and "a" are short for "tr"/"rt", "tw"/"wt" and "ta"/"at"
# the t here stands for text, as opposed to binary, but default mode is text if unspecified so we don't need t
